<style></style>

<template>
    <thead class="${props.class.join(' ')}">
        <ui-trow items="${props.fields}"></ui-trow>
    </thead>
</template>

<script>
import {Component} from 'vcomponent';
import Trow from './Trow';

export default class Thead extends Component {
    ready() {}

    getComponentClasses() {
        return [Trow];
    }
}
</script>
